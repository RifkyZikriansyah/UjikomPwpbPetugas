<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ujikompwpbpetugas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LookActivity"></activity>
        <activity android:name=".LoginFragment" />
        <activity android:name=".RegisterFragment" />
        <activity android:name=".MainActivity" />
        <activity android:name=".HomeFragment" />
        <activity android:name=".LookFragment" />
        <activity android:name=".MakeFragment" />
        <activity android:name=".ProfileFragment" />
        <activity android:name=".ClearLookFragment" />
        <activity android:name=".ProcessLookFragment" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>